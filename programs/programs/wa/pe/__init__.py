"""PolicyEngine calculators for WA.

Each calculator in `member`, `spm` and `tax` declares the ``Program`` row it backs
and is found by `programs.framework.registry`, so there is nothing to list here.
"""
